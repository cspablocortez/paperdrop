<%= erb :"notes/_form" %> 

<hr>

<h1>All Notes</h1>

<ul>
    <% @notes.each do |note| %>
    <li><a href=<%= "/notes/#{note.id}" %>><%= note.title %></a></li>
    <p><%= note.content %></p>
    <% end %>
</ul>

